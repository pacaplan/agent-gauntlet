base_branch: origin/main
log_dir: .gauntlet_logs
allow_parallel: true
cli:
  default_preference:
    - claude
    - codex
    - gemini
  check_usage_limit: false
entry_points:
  - path: "src"
    checks:
      - test
      - lint
      - security-code
    reviews:
      - code-quality
  - path: "package.json"
    checks:
      - security-deps
  - path: "docs/plans"
    reviews:
      - plan-review
